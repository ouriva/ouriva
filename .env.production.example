# ============================================================
# Ouriva — Docker Quick Start Environment
# ============================================================
# Copy this file to .env and set your database connection
# string. Used by docker-compose.yml (the external-database
# variant) — see the README's "Quick Start (Docker)" section.
#
# Usage:
#   cp .env.production.example .env
#   # edit .env with your actual DATABASE_URL
# ============================================================

DATABASE_URL="postgresql://db_user:db_password@db_host:5432/ouriva"
